// Point this to your backend (see /backend). For Android emulator use 10.0.2.2,
// for a physical device use your machine's LAN IP.
export const API_BASE_URL = __DEV__
  ? 'http://10.0.2.2:3000/api/v1'
  : 'https://api.nimadkirana.com/api/v1';

export const APP_NAME = 'Nimad Kirana';
