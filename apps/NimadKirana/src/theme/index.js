import colors from './colors';
import { fontSize, fontWeight, spacing, radius } from './typography';

export const theme = {
  colors,
  fontSize,
  fontWeight,
  spacing,
  radius,
};

export { colors, fontSize, fontWeight, spacing, radius };
export default theme;
